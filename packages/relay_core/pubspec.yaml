name: relay_core
description: Platform-neutral Token Cost Relay Protocol v1 client primitives.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  http: ^1.2.0
  pointycastle: ^3.9.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
