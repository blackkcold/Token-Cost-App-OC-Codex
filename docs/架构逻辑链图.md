# 架构逻辑链图

## 系统全景架构

```mermaid
graph TB
    subgraph "🏗️ 三层架构"
        direction TB
        subgraph "🖥️ Views 展示层"
            CTV[Content View<br/>TabView 容器]
            TV[Total View<br/>双源总计页]
            OPV[OpenCode Page View<br/>侧边栏+明细]
            CPV[Codex Page View<br/>Session 趋势+列表]
            SV[Settings View<br/>设置面板]
            MBV[MenuBar View<br/>菜单栏]
        end

        subgraph "🗄️ Stores 状态层"
            TCM[Token Cost Model<br/>OpenCode 数据层]
            CSM[Codex Session Model<br/>Codex 数据层]
            APM[App Preferences Model<br/>语言 + 总览计价 + 计费方案]
            UCM[Update Checker Model<br/>版本更新检查]
            SS[Snapshot Store<br/>快照持久化]
            CSS[Codex Snapshot Store<br/>Codex 快照]
        end

        subgraph "⚙️ Core 核心模块"
            TDC[Token Database Client<br/>SQLite 客户端]
            SDS[Source Discovery Service<br/>来源发现]
            DA[Dashboard Analytics<br/>分析引擎+Provider 成本覆盖]
            THB[Token Heatmap Builder<br/>双源每日 token 热力图]
            BPC[Billing Plan Catalog<br/>订阅/Token Plan 目录<br/>billingOverrides 覆盖全部 5 Provider]
            CA[Codex Analytics<br/>Codex 分析]
            SET[Settings Store<br/>设置持久化]
            LOC[Localization<br/>bundle + locale]
            SFS[Safe File Store<br/>安全文件读写]
            SP[Source Profiles<br/>来源配置]
            AP[App Preferences<br/>本地偏好存储]
            UC[Update Checker<br/>GitHub API + 下载 + 解压]
            BM[Balance Manager<br/>余额监控核心]
            CBS[Credential Bootstrap Service<br/>凭证引导]
            CCV[Credential Candidate Validator<br/>凭证候选验证]
            BCE[Browser Cookie Extractor<br/>浏览器 Cookie 提取]
        end
    end

    CTV --> TV
    CTV --> OPV
    CTV --> CPV
    OPV --> TCM
    CPV --> CSM
    CTV --> TCM
    CTV --> CSM
    CTV --> APM
    CTV --> UCM
    UCM --> UC
    TCM --> SDS
    TCM --> TDC
    TCM --> SS
    TCM --> SET
    CSM --> CHR[Codex Helper Runner]
    CSM --> CSS
    APM --> AP
    APM --> LOC
    APM --> BPC
    BPC --> DA
    CHR --> CH[Codex Token Cost Helper<br/>外部进程]
    CH --> CSC[Codex Session Collector]
    DA --> TCM
    CA --> CSM
    THB --> TV
    BM --> CBS
    CBS --> CCV
    CBS --> BCE
    UC -->|签名 manifest| BM
```

## 双数据源流

```mermaid
flowchart LR
    subgraph "Source 1: OpenCode"
        SQL[(SQLite DB<br/>opencode.db)]
        SDS1[Source Discovery Service]
        TDC1[Token Database Client]
        DSL1[SQL: json_extract]
        RAW1[Usage Aggregate Rows]
        DA1[Dashboard Analytics]
        V1[Views: 侧边栏+明细]
    end

    SQL --> SDS1 --> TDC1
    TDC1 --> DSL1 --> RAW1
    RAW1 --> DA1 --> V1
```

```mermaid
flowchart LR
    subgraph "Source 2: Codex"
        JSONL[(JSONL Files<br/>session/*.jsonl)]
        CSC[Codex Session Collector]
        CSV[Codex Helper Runner<br/>子进程调用]
        RAW2[Codex Dashboard Payload]
        CA2[Codex Analytics]
        V2[Views: 趋势+Session 列表]
    end

    JSONL --> CSV --> CSC --> RAW2
    RAW2 --> CA2 --> V2
```

## 数据模型层级

```mermaid
classDiagram
    class TokenCostSource {
        +String id
        +String name
        +TokenCostSourceFamily sourceFamily
        +TokenCostSourceLocationKind locationKind
        +URL sourceURL
        +TokenCostSourceStatus status
    }

    class DashboardPayload {
        +Summary summary
        +Dict dailyTotals
        +Dict modelTotals
        +Dict providerCosts
        +Dict providerTotals
        +[RawRow] rawData
    }

    class CodexDashboardPayload {
        +Summary summary
        +[CodexSessionSummary] sessions
    }

    class TokenCostDashboardAnalytics {
        +Overview overview
        +CacheSummary cache
        +[ProviderRankRow] providerRankRows
        +[ModelComparisonRow] modelComparisonRows
        +[DistributionSlice] modelSlices
        +[TrendPoint] trendPoints
        +[StackedSeries] stackedSeries
    }

    class CodexSessionSummary {
        +String sessionId
        +String label
        +CodexTokenUsage usage
        +Int tokenCountEvents
    }

    class TokenCostSettings {
        +TokenCostSourceFamily sourceFamily
        +[String] sourceRoots
        +[String] manualSourcePaths
        +Bool autoRescan
        +TokenCostThemeChoice theme
    }

    class AppPreferences {
        +AppDisplayLanguage language
        +Dict billingSelectionsByProvider
        +TokenCostThemeChoice theme
        +DisplayCurrency displayCurrency
        +DeveloperModePreferences developerMode
    }

    class BillingPlanCatalog {
        +[BillingPlanPreset] presets
        +resolve(provider, selection)
        +billingOverridesByProviderKey()
    }

    class AppPreferencesStore {
        +load()
        +save()
    }

    TokenCostSource --> TokenCostSettings
    AppPreferencesStore --> AppPreferences
    AppPreferences --> BillingPlanCatalog
    DashboardPayload --> TokenCostDashboardAnalytics
    CodexDashboardPayload --> CodexSessionSummary
```

## UI 导航树

```
Token Cost App (@main)
├── TabView: ContentView
│   ├── Tab: 总计 → TotalView
│   │   └── Cards: 总成本 | OpenCode合计 | 每日用量热力图 | Codex合计
│   │
│   ├── Tab: opencode → OpenCodePageView
│   │   └── NavigationSplitView
│   │       ├── Sidebar: SidebarView
│   │       │   ├── 来源列表 (TokenCostSource rows)
│   │       │   └── 状态指示器
│   │       └── Detail: DetailView
│   │           ├── 总览 (Overview)
│   │           ├── 每日趋势 (Shared Trend Chart)
│   │           ├── 缓存分析 (Cache Section)
│   │           ├── Provider 排行 (Ranking)
│   │           ├── 模型对比 (Model Comparison)
│   │           ├── 分布图 (Pie Charts)
│   │           ├── 每日堆叠 (Stacked Bar)
│   │           └── 明细表 (Detail Table)
│   │
│   └── Tab: codex → CodexPageView
│       ├── 总览卡片
│       ├── 每日趋势 (Shared Trend Chart)
│       └── Session 列表 (分页+排序)
│
├── Settings → SettingsView (NavigationSplitView + Sidebar)
│   ├── 侧边栏: 9 个分区选择
│   │   ├── 概览 (Summary Cards + 刷新全部)
│   │   ├── 偏好 (语言 + 币种 + 主题)
│   │   ├── 计费 (Provider 预设 + 定价文档)
│   │   ├── 余额 (监控开关 + Provider 开关 + Go 凭证)
│   │   ├── OpenCode (扫描配置 + 目录/文件管理)
│   │   ├── Codex (Session 来源 + 发现)
│   │   ├── Skills (显示偏好)
│   │   ├── 安全 (恢复默认)
│   │   └── 开发者模式 (子功能开关 + 本地治理)
│   └── 详情区: 当前选中分区的具体设置
│
└── MenuBar → MenuBarView
```

## 关键文件索引

| 文件 | 职责 | 行数 |
|------|------|------|
| `Models.swift` | TokenCost 核心数据模型 | 386 |
| `CodexModels.swift` | Codex session 数据模型 | 160 |
| `DashboardAnalytics.swift` | OpenCode 分析引擎 + 模型定价表 | 819 |
| `CodexAnalytics.swift` | Codex 分析引擎（趋势/排序） | 222 |
| `TokenHeatmapBuilder.swift` | OpenCode + Codex 双源热力图数据构建 | 新增 |
| `AppPreferences.swift` | 全局偏好、计费选择和本地持久化 | 新增 |
| `BillingPlanCatalog.swift` | Provider 订阅 / Token Plan 目录和自定义费用解析 | 新增 |
| `Localization.swift` | 本地化 bundle 解析与文本格式化 | 新增 |
| `TokenDatabaseClient.swift` | SQLite 直连查询 | 379 |
| `SourceDiscoveryService.swift` | 文件系统扫描发现来源 | 209 |
| `CodexSessionCollector.swift` | JSONL session 解析器 | 607 |
| `TokenCostModel.swift` | OpenCode 数据 Store | 328 |
| `CodexSessionModel.swift` | Codex 数据 Store | 641 |
| `AppPreferencesModel.swift` | 全局偏好状态桥接 | 新增 |
| `SettingsStore.swift` | 设置 + 快照持久化 | 127 |
| `SafeFileStore.swift` | 安全沙箱内文件操作 | 84 |
| `LocalEncryptedCredentialStore.swift` | 本地 AES-256-GCM 加密凭证存储（CryptoKit，无 Keychain） | 309 |
| `LocalCredentialService.swift` | Core 凭证 facade，包装加密存储 + 显式 legacy Keychain 导入 | 248 |
| `SecureCredentialStore.swift` | 旧版 Keychain 凭证存储（仅作为显式 legacy 导入源） | 497 |
| `DetailView.swift` | 最复杂的视图（1028行） | 1028 |
| `TrendChartView.swift` | OpenCode / Codex 共享趋势图和 7/30 日切换 | 新增 |
| `TokenHeatmapGrid.swift` | 总计页响应式 52 周热力图 | 新增 |
| `SettingsView.swift` | 设置界面（NavigationSplitView + 9 分区侧边栏） | ~1800 |
| `PricingDocView.swift` | 内置计费文档只读查看器 | 新增 |
| `CodexPageView.swift` | Codex 详情页 | 504 |
| `build_and_run_codex.sh` | 构建脚本 | 238 |
| `UpdateChecker.swift` | 版本检查、Ed25519 签名 manifest 验证、流式下载与安装服务 | ~450 |
| `UpdateCheckerModel.swift` | 更新状态机 Store | 新增 |
| `CredentialBootstrapService.swift` | 会话级凭证引导：本地加密缓存优先 → 浏览器/Profile 验证链 | ~360 |
| `CredentialCandidateValidator.swift` | 凭证候选验证协议和 LiveCredentialCandidateValidator 实现 | ~145 |
| `BrowserCookieExtractor.swift` | 浏览器 Cookie 提取，Security.framework 静默读取，候选列表化 | ~310 |
| `BalanceManager.swift` | 余额监控核心，ProviderOutcome 枚举区分 cancelled/timeout/snapshot | ~430 |
| `BalanceModels.swift` | 余额数据模型（BalanceSnapshot/BalanceQuotaWindow 等） | ~280 |
| `BalanceRefreshScheduler.swift` | 应用生命周期级余额刷新调度 | ~120 |
| `BalanceFloatingPanelCoordinator.swift` | 悬浮余额面板 NSPanel 协调器 | ~180 |
| `BalanceFloatingPanelView.swift` | 悬浮面板 SwiftUI 主视图 | ~220 |
| `LiquidGlassButtonStyle.swift` | SwiftUI ButtonStyle，非彩色按压反馈 | ~40 |
| `VisualEffectBackground.swift` | NSVisualEffectView SwiftUI 封装 | ~50 |
| `WindowLifecycleManager.swift` | 窗口生命周期管理与 Dock 图标策略 | ~90 |
| `WindowOpeningSupport.swift` | 统一窗口打开入口 | ~40 |
